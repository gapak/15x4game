
function Action(name, text, requires, code) {
    this.name = name;
    this.text = text;
    this.requires = requires;
    this.code = code;

    this.is_reached = is_reached;




    return this;
}